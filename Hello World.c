#include <stdio.h>     // Seleciona a biblioteca

int main()             //A função main deve ser do tipo int, e nela é retornado 0 para indicar para o sistema que o programa foi encerrado corretamente
{
   printf("Hello world");    //Printf é usado para escrever o que esta entre as ""
}
